server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:5333/market?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: market
    password: market123
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    # password:    # 如有密码则写
    database: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: market-system-jwt-secret-key-2024-very-long-string
  expiration: 86400000   # 24小时